[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
markers =
    database: Database schema and integrity tests
    backend: Backend API and service tests
    frontend: Frontend HTML and UI tests
    security: Security and vulnerability tests
    openai: Tests requiring real OpenAI API calls
